export const ArenaCard = () => {
    return (
        <div>
            {/* Card detail */}
            <div>
                {arena.map(() => (

                {/* Sport category */}
                <div>
                    {ArenaCard.category}
                </div>
                ))}
            </div>
        </div>
    )
}