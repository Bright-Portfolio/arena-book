const Navbar = () => {
  return (
    <nav className="flex flex-row w-full bg-red-100">
      <div className="self-end">
        <button className="text-black">Book Now</button>
      </div>
    </nav>
  );
};

export default Navbar;
