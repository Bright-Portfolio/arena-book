const HeroSection = () => {
  return (
    <div
      className="bg-cover bg-center w-full h-screen"
      style={{
        backgroundImage: "url('/images/tennis-hero-bg.jpg')",
      }}
    >
      Hello there
    </div>
  );
};

export default HeroSection;
