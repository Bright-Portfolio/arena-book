import pool from "@/lib/db";
import {
  CreateArenaInputSchema,
  CreateArenaOutputSchema,
} from "../validators/arena.schema";

/**
 * Upsert arena info
 *
 */

export async function upsert(params: type) {}
